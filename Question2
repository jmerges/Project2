f = load('mnist_all.mat');
T = zeros(10,784);
for i = 0:9
    x = f.(sprintf('train%d', i));
    T(i+1,:) = mean(x);
end


A = zeros(28,28);
for i = 1:28
    A (:,i) = T(1,(28*i-1)+1:(28*i));
end
image(A)
